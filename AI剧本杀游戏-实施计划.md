# AI剧本杀游戏实施计划

**版本：** V1.0  **状态：** 待评审  **依据：** 产品需求文档 V1.0.0、技术与实施方案 V1.0、实施计划生成提示词

## 1. 实施计划摘要

本计划以 Python FastAPI、React TypeScript、PostgreSQL、Redis/队列、Docker 为基线，采用“先确定性规则和数据权限、再接入模型、最后完善编辑器与生成内容”的顺序。MVP 目标是完成 3 个可玩的单人剧本，支持角色对话、三层防泄露、证物、笔记、AI 搭档、结算、真相揭示、自动存档和 GPT/DeepSeek 两个 Provider。多人联机和社交系统不在范围内。

**建议周期：** 10—12 周，实际工期需根据人员数量、剧本素材、模型额度和部署环境验证。  
**关键路径：** M0 → 数据模型 → 内容 Schema/规则引擎 → 会话状态 → 角色上下文 → 防泄露 → 流式交付 → 结算 → E2E。  
**可并行工作：** 基础 UI、内容包编写、模型 Provider、测试夹具和部署脚本可在数据契约冻结后并行。

## 2. 输入假设与冲突说明

- PRD 的“首 token ≤2 秒”与审核后展示存在冲突：分别测量原始模型 TTFT 和审核后首块延迟，审核后首块暂定 P95 ≤12 秒，待 M0 实测确认。
- 角色、证物、玩家知识、评分和结局由服务端权威规则控制，LLM 不能直接修改。
- MVP 采用审核完成后语义块流式显示，不透传未审核 token。
- `api/`、`web/`、`docs/`、`data/`、`Makefile` 为实际项目结构；当前实施不强制拆分微服务。
- 精确人日、Provider 价格和地区可用性均标记为待评估。

## 3. 项目角色

| 角色 | 职责 |
|---|---|
| 产品负责人 | 范围、优先级、验收和剧本签收 |
| 技术负责人 | 架构、代码规范、发布门禁和风险决策 |
| 后端工程师 | FastAPI、数据库、规则、会话和权限 |
| AI 工程师 | Provider、上下文、审核、评测和成本 |
| 前端工程师 | React 页面、SSE、笔记、编辑器和响应式交互 |
| 测试工程师 | 自动化、攻击集、兼容、性能和人工验收 |
| 内容/编剧 | 剧本、证据链、对话、结局和质检 |
| 运维工程师 | Docker、密钥、监控、备份和发布 |

## 4. 阶段和里程碑

| 阶段 | 里程碑 | 计划窗口 | 退出条件 |
|---|---|---:|---|
| 0 | 技术验证完成 | W1-W2 | Provider、取消、审核闭环可运行 |
| 1 | 数据库基础完成 | W2-W4 | 迁移、权限、种子和幂等通过 |
| 2 | 内容规则可执行 | W3-W5 | 内容包编译、触发器、质检通过 |
| 3 | 单人调查闭环 | W4-W8 | 可从开局走到结局 |
| 4-5 | 角色与防泄露可用 | W5-W8 | 攻击集无核心越权外发 |
| 6-7 | 证物、笔记、搭档 | W6-W9 | 证据链和引用可复核 |
| 8-10 | 编辑器、媒体、页面 | W7-W10 | 创作和主要页面可用 |
| 11-12 | 质量与发布 | W10-W12 | 三个剧本验收、回滚演练完成 |

## 5. 详细任务清单

以下任务采用统一字段；同阶段内按编号顺序执行。预计工作量使用“待评估”表示，负责人为角色而非具体人员。

### 阶段 0：初始化与技术验证

#### TASK-001 仓库与分支初始化

- **所属阶段：** 阶段 0；**优先级：** P0；**任务类型：** 运维/工程
- **任务目标：** 建立可重复开发、评审和发布的仓库基础。
- **具体需求：** 创建 `api/`、`web/`、`docs/`、`data/`、`Makefile`；约定主干、功能分支、提交和 PR 检查；加入 `.gitignore`、许可证和安全报告流程。
- **前置依赖：** 无。**输入：** 技术目录方案。**输出：** 可克隆仓库和贡献指南。
- **涉及文件：** `README.md`、`Makefile`、`.gitignore`、`.github/`、`docs/adr/`。
- **实现步骤：** 初始化仓库；添加 CI 占位；写本地启动命令；配置依赖扫描。
- **验收标准：** 新环境可按 README 启动；PR 必须通过格式和测试检查。
- **测试要求：** 干净环境复现安装。
- **风险：** 规则过早变化；**回滚：** ADR 记录并版本化调整。
- **预计工作量：** 待评估；**负责人角色：** 技术负责人/运维。
- **完成定义：** 文档、CI、分支保护和启动验证均签收。

#### TASK-002 FastAPI、React、Docker 基线

- **所属阶段：** 阶段 0；**优先级：** P0；**任务类型：** 后端/前端/运维
- **任务目标：** 建立前后端最小可运行服务。
- **具体需求：** FastAPI `main.py`、React Vite 入口、Dockerfile、Compose、健康检查和环境配置；禁止真实密钥入库。
- **前置依赖：** TASK-001；**输入：** Python/React 目录约束；**输出：** `/health`、前端首页和容器编排。
- **涉及文件：** `api/main.py`、`api/settings.py`、`api/requirements.txt`、`api/Dockerfile`、`web/package.json`、`web/Dockerfile`、`.env.example`。
- **实现步骤：** 配置启动；接入统一错误响应；配置 CORS、日志和请求 ID；Compose 联调。
- **验收标准：** 前端可访问 API 健康检查；错误不泄露堆栈；容器重启可恢复。
- **测试要求：** API 冒烟、容器启动、配置缺失测试。
- **风险：** 版本不兼容；**回滚：** 锁定已验证版本。
- **预计工作量：** 待评估；**负责人角色：** 后端/前端/运维。
- **完成定义：** CI 中构建并启动通过。

#### TASK-003 PostgreSQL、Redis 与 LLM Provider Spike

- **所属阶段：** 阶段 0；**优先级：** P0；**任务类型：** AI/后端/运维
- **任务目标：** 验证数据库、队列和 GPT/DeepSeek 最小调用。
- **具体需求：** Provider 统一接口、超时、错误归一化、token 计量；测试流式、取消、限时和模拟 Provider。
- **前置依赖：** TASK-002；**输入：** Provider 配置和测试密钥；**输出：** `api/llm_service.py` 初版、Spike 报告。
- **涉及文件/API：** `api/llm_service.py`、`api/settings.py`、`/health/dependencies`。
- **实现步骤：** 定义 `generate/stream/cancel`；接入两个提供方；实现模拟响应和调用账本占位。
- **验收标准：** 两个 Provider 均能成功、超时、取消；不支持流式时能降级为轮询或完整响应。
- **测试要求：** Mock、真实小额调用、故障注入。
- **风险：** 地区/额度/接口变化；**回滚：** 切换模拟 Provider 或已验证备选配置。
- **预计工作量：** 待评估；**负责人角色：** AI 工程师。
- **完成定义：** 延迟、成本、错误和能力报告完成。

#### TASK-004 三层审核和流式最小闭环

- **所属阶段：** 阶段 0；**优先级：** P0；**任务类型：** AI/后端
- **任务目标：** 证明候选生成、批评、修订、语义块交付和取消可协作。
- **具体需求：** 未审核草稿不得进入 SSE；失败进入安全回避；分别记录原始 TTFT 和审核后首块。
- **前置依赖：** TASK-003；**输入：** 合成角色和秘密；**输出：** `run_id`、状态事件和最小评测结果。
- **涉及文件/API：** `api/llm_service.py`、`api/invoke_types.py`、SSE `/runs/:id/events`。
- **实现步骤：** 定义状态机；实现安全候选；检测直接泄露；修订复检；持久化批准块。
- **验收标准：** 直接索要答案不会交付；取消不再产生新块；断线可按游标恢复。
- **测试要求：** 直接/注入/多轮攻击与合法披露测试。
- **风险：** 审核延迟；**回滚：** 关闭模型正文，仅显示安全状态和模板。
- **预计工作量：** 待评估；**负责人角色：** AI 工程师/后端。
- **完成定义：** M0 风险清单与继续/调整决策签字。

### 阶段 1：数据模型与数据库

#### TASK-005 核心 ORM、迁移和身份

- **所属阶段：** 阶段 1；**优先级：** P0；**任务类型：** 数据库/后端
- **任务目标：** 建立用户、游客、剧本版本和会话基础。
- **具体需求：** SQLAlchemy 模型、迁移、复合唯一键、版本冻结、所有权校验、状态版本。
- **前置依赖：** TASK-002；**输入：** 数据模型章节；**输出：** `models.py`、`db.py`、`schema.sql`、迁移脚本。
- **涉及表：** User、Guest、Script、ScriptVersion、GameSession、SessionSnapshot、InvestigationEvent。
- **实现步骤：** 定义字段和索引；事务 Session；游客令牌；快照和事件追加；种子数据。
- **验收标准：** 重复命令幂等；旧版本存档不受新版本影响；未授权会话不可读。
- **测试要求：** 迁移、回滚、并发、所有权和恢复测试。
- **风险：** Schema 变更；**回滚：** 扩展—迁移—收缩，保留旧字段。
- **预计工作量：** 待评估；**负责人角色：** 后端/数据库。
- **完成定义：** 测试数据库可从零迁移并清理。

#### TASK-006 内容、证物、对话、笔记与结算表

- **所属阶段：** 阶段 1；**优先级：** P0；**任务类型：** 数据库/后端
- **任务目标：** 完成所有运行时数据表和审计账本。
- **具体需求：** Character、Fact、KnowledgeGrant、Relationship、EvidenceDefinition/Instance、Trigger、DialogueRun/MessageChunk、Note、PartnerAnalysis、Submission/Ending、Asset、Job、UsageLedger、Outbox、Audit。
- **前置依赖：** TASK-005；**输入：** 数据模型和内容协议；**输出：** ORM、索引、迁移、合成数据。
- **涉及文件：** `api/models.py`、`api/evidence_models.py`、`api/schema.sql`。
- **实现步骤：** 定义外键与状态值；增加敏感字段隔离；写 Repository 查询；加入审计和使用量。
- **验收标准：** 所有表有主键、必要外键、索引和状态迁移；私密事实不能由玩家查询返回。
- **测试要求：** 外键、权限投影、重复 Outbox、数据删除测试。
- **风险：** 过度 JSONB；**回滚：** 保留结构化关键列并版本化 JSON。
- **预计工作量：** 待评估；**负责人角色：** 数据库/后端。
- **完成定义：** 迁移和实体关系图评审通过。

### 阶段 2：内容模型与规则引擎

#### TASK-007 内容包 Schema、编译和冻结

- **所属阶段：** 阶段 2；**优先级：** P0；**任务类型：** 内容/后端
- **任务目标：** 让剧本以可验证版本包运行。
- **具体需求：** JSON/YAML manifest、角色、事实、场景、证物、触发器、结局和评分 Schema；禁止 `eval`、任意 JavaScript 和用户脚本。
- **前置依赖：** TASK-006；**输入：** 合成剧本；**输出：** 编译器、校验报告、冻结 checksum。
- **涉及文件：** `api/invoke_types.py`、`data/`、`docs/content-schema.md`。
- **实现步骤：** Schema 校验；引用解析；ID 唯一检查；敏感级别；版本冻结和导入 API。
- **验收标准：** 非法引用、路径穿越、无结局和越权授权被阻断；冻结版本不可原地修改。
- **测试要求：** 合法/非法包、迁移、大小和资源类型测试。
- **风险：** 动态内容不可验证；**回滚：** 退回最近冻结版本。
- **预计工作量：** 待评估；**负责人角色：** 后端/内容。
- **完成定义：** 至少一个合成包可编译并安装。

#### TASK-008 条件 AST、事件和证据规则引擎

- **所属阶段：** 阶段 2；**优先级：** P0；**任务类型：** 后端
- **任务目标：** 确定性推进场景、线索、情绪、信任和结局。
- **具体需求：** `all/any/not`、阶段、拥有证物、信任度、关键词、时间条件；效果包括解锁事实、状态变化、触发事件和结局，不执行任意代码。
- **前置依赖：** TASK-007；**输入：** 内容包；**输出：** `engine` 规则模块和可重放事件。
- **涉及 API/表：** actions、PlayerKnowledge、CharacterState、EvidenceInstance、InvestigationEvent。
- **实现步骤：** 解释器；幂等效果；状态版本；组合/出示；多结局优先级；质检遍历。
- **验收标准：** 同输入同版本得到同结果；重复行动不重复发放；关键线索可达。
- **测试要求：** 属性测试、循环依赖、并发和回放测试。
- **风险：** 规则复杂度；**回滚：** 规则版本固定，旧会话继续使用旧引擎。
- **预计工作量：** 待评估；**负责人角色：** 后端/测试/内容。
- **完成定义：** 规则覆盖率和质检报告达标。

### 阶段 3：单人游戏核心流程

#### TASK-009 会话创建、选本与身份分配

- **所属阶段：** 阶段 3；**优先级：** P0；**任务类型：** 后端/前端
- **任务目标：** 完成从剧本详情到可调查会话。
- **具体需求：** 创建 session、绑定 ScriptVersion、视角、难度、seed；返回玩家可见投影，不返回答案。
- **前置依赖：** TASK-005、TASK-007；**输入：** 剧本 ID 和配置；**输出：** session 状态、角色列表、初始场景。
- **涉及 API/表/页面：** `POST /sessions`；GameSession、CharacterState；`ScriptLibrary.tsx`、`PlayScript.tsx`。
- **实现步骤：** 所有权校验；初始化状态；写开局事件；生成首个快照；断点续玩。
- **验收标准：** 3 分钟内可开局；二次请求不重复创建；私密角色信息不在响应。
- **测试要求：** 游客、正式用户、错误版本和重复请求。
- **风险：** 版本下架；**回滚：** 保留已创建会话，阻止新局。
- **预计工作量：** 待评估；**负责人角色：** 后端/前端。
- **完成定义：** 可从剧本库进入调查页。

#### TASK-010 场景、调查行动和角色列表

- **所属阶段：** 阶段 3；**优先级：** P0；**任务类型：** 后端/前端
- **任务目标：** 实现调查阶段的确定性行动。
- **具体需求：** 场景查看、搜索、观察、监控、比对、重演；行动点/时间、前置条件、证物和角色状态。
- **前置依赖：** TASK-008、TASK-009；**输入：** 场景和触发器；**输出：** 状态事件和玩家投影。
- **涉及 API/表/页面：** `GET /sessions/:id/state`、`POST /actions`；InvestigationEvent、EvidenceInstance；`PlayScript.tsx`、`Actor.tsx`。
- **实现步骤：** 命令校验；事务追加事件；更新 snapshot；SSE/查询刷新。
- **验收标准：** 重复行动幂等；不满足条件不发证物；离线意图恢复后重新校验。
- **测试要求：** 并发、多标签页、断网和状态冲突。
- **风险：** 前端状态过期；**回滚：** 以服务端序号重载。
- **预计工作量：** 待评估；**负责人角色：** 后端/前端。
- **完成定义：** 至少一个场景完成调查闭环。

#### TASK-011 角色提问、上下文与流式回答

- **所属阶段：** 阶段 3；**优先级：** P0；**任务类型：** AI/后端/前端
- **任务目标：** 实现可审核的角色流式对话。
- **具体需求：** 问题不可信输入；构建授权上下文；创建 DialogueRun；批准块 SSE；取消、重试、继续和游标恢复。
- **前置依赖：** TASK-004、TASK-009、TASK-010；**输入：** 角色 ID、问题、session version；**输出：** run 状态和 MessageChunk。
- **涉及 API/表/页面：** `POST /dialogue-runs`、`GET /runs/events`、`POST /cancel`；DialogueRun、MessageChunk；`Actor.tsx`、`api/`。
- **实现步骤：** 状态机；事务写块和知识；SSE 心跳；恢复和并发运行槽；前端打字动画。
- **验收标准：** 未审核草稿不外发；停止后无新批准块；刷新可恢复且不重复解锁。
- **测试要求：** Provider 超时、取消竞态、SSE 断线、重复 run。
- **风险：** 审核延迟；**回滚：** 安全模板或仅显示状态。
- **预计工作量：** 待评估；**负责人角色：** AI/后端/前端。
- **完成定义：** 一个角色可多轮调查并保持状态。

#### TASK-012 证物、笔记、搭档与结算闭环

- **所属阶段：** 阶段 3；**优先级：** P0；**任务类型：** 后端/前端/AI
- **任务目标：** 把调查结果转化为推理和结局。
- **具体需求：** 证物发现/分析/组合/出示；笔记自动保存；搭档读取授权投影；提交答案、确定性评分、分章节真相和多周目。
- **前置依赖：** TASK-008、TASK-010、TASK-011；**输入：** 证据链和 Rubric；**输出：** 结局、复盘和存档。
- **涉及 API/表/页面：** evidence、notes、partner、submissions、reveal；EvidenceInstance、Note、PartnerAnalysis、Result；`EnhancedNotesPanel.tsx`、`MultipleChoiceGame.tsx`。
- **实现步骤：** 建立来源引用；搭档权限；终局锁定；评分和结局；复盘查询。
- **验收标准：** 搭档不读系统真相；分数可复算；错误答案也能揭示对应反馈；重玩新建 session。
- **测试要求：** 引用越权、自动保存冲突、重复提交和结局分支。
- **风险：** LLM 点评影响判分；**回滚：** 结构化评分为唯一正式结果。
- **预计工作量：** 待评估；**负责人角色：** 后端/AI/前端/内容。
- **完成定义：** 三本剧本至少一条成功和一条错误结局可走通。

### 阶段 4：多智能体角色系统

#### TASK-013 角色状态、记忆与策略

- **所属阶段：** 阶段 4；**优先级：** P0；**任务类型：** AI/后端
- **任务目标：** 使角色具备独立人格、记忆和状态策略。
- **具体需求：** 人格、语气、私有知识投影、情绪、信任、警觉、历史陈述、承诺说法、摘要、真话/谎言/回避/失控策略。
- **前置依赖：** TASK-006、TASK-008、TASK-011；**输入：** 角色配置和事件；**输出：** Agent Context Builder 与 CharacterState 更新命令。
- **涉及文件：** `api/llm_service.py`、`api/models.py`；CharacterState、MemorySummary、CharacterClaim。
- **实现步骤：** 分层记忆；长度预算；状态变化规则；角色上下文单元测试；禁止 Agent 直接改权威状态。
- **验收标准：** 角色只访问授权信息；关键陈述可追溯；状态变化有事件原因。
- **测试要求：** 多轮一致性、跨角色越权、摘要压缩和长上下文。
- **风险：** 摘要创造新事实；**回滚：** 仅使用结构化事实和最近原文。
- **预计工作量：** 待评估；**负责人角色：** AI 工程师。
- **完成定义：** 一致性评测通过并接入对话链。

### 阶段 5：三层 AI 防泄露

#### TASK-014 授权投影与禁止披露集

- **所属阶段：** 阶段 5；**优先级：** P0；**任务类型：** AI/安全/后端
- **任务目标：** 对角色和搭档生成最小可用上下文。
- **具体需求：** `known_fact_ids`、`usable_private_fact_ids`、`releasable_fact_ids`、禁用事实、多轮累计知识；客户端输入视为不可信。
- **前置依赖：** TASK-006、TASK-008、TASK-013；**输入：** session、角色、阶段和权限；**输出：** 授权投影和审计摘要。
- **涉及 API/表：** dialogue、partner；KnowledgeGrant、PlayerKnowledge、Audit。
- **实现步骤：** policy 函数；字段白名单；敏感级别；拒绝默认；投影快照。
- **验收标准：** 角色/搭档无法读取未授权答案；投影与事件版本一致。
- **测试要求：** IDOR、提示注入、跨角色和跨会话访问测试。
- **风险：** 错误授权导致卡关；**回滚：** 回退到更严格投影并人工解锁。
- **预计工作量：** 待评估；**负责人角色：** AI/安全/后端。
- **完成定义：** 权限测试零高危失败。

#### TASK-015 生成、检测、修订、复检和安全降级

- **所属阶段：** 阶段 5；**优先级：** P0；**任务类型：** AI/安全
- **任务目标：** 达到防泄露与延迟指标的可测实现。
- **具体需求：** 直接泄露、越权、时间线、知识一致性、多轮组合和 Prompt Injection 检测；最多两轮修订；安全回避；记录 TTFT、审核首块、完成延迟、拦截率、攻击泄露率、合法误拦截率。
- **前置依赖：** TASK-014；**输入：** 候选、允许披露集和禁止集；**输出：** Approved/Rejected/Retry 状态。
- **涉及文件：** `api/llm_service.py`、`api/invoke_types.py`；DialogueRun、Audit、UsageLedger。
- **实现步骤：** 规则检测；模型判定仅作辅助；修订 Prompt；复检；失败不旁路；指标上报。
- **验收标准：** 核心回归集无越权外发；合法披露误拦截统计独立；审核故障不放行原稿。
- **测试要求：** 至少 300 条攻击、200 条合法披露，覆盖多轮和多角色；具体阈值待 M0 确认。
- **风险：** 漏检/误拦截；**回滚：** 安全模板、关闭自由回答或切换已评测模型。
- **预计工作量：** 待评估；**负责人角色：** AI/安全/测试。
- **完成定义：** 评测报告、阈值、版本和人工抽检记录齐备。

### 阶段 6：证物系统

#### TASK-016 证物 API、状态和角色反应

- **所属阶段：** 阶段 6；**优先级：** P0；**任务类型：** 后端/前端
- **任务目标：** 完成证物全生命周期。
- **具体需求：** 定义、实例、发现、详情、分析、组合、出示、事实/线索/事件关联和角色反应。
- **前置依赖：** TASK-008、TASK-010；**输入：** evidence 配置；**输出：** `evidence_api.py`、组件和事件。
- **涉及文件/API：** `api/evidence_models.py`、`api/evidence_api.py`；`GET/POST /evidence`；`components/evidence/`。
- **实现步骤：** 规则前置校验；幂等命令；出示触发器；可信度与玩家判断分离；受控资源 URL。
- **验收标准：** 未发现证物不可查；组合不重复消耗；角色反应和新线索可追踪。
- **测试要求：** 组合顺序、重复出示、权限和资源下载测试。
- **风险：** 图片与事实不一致；**回滚：** 使用文字证物或旧版资产。
- **预计工作量：** 待评估；**负责人角色：** 后端/前端/内容。
- **完成定义：** 三类证物完成发现到出示闭环。

### 阶段 7：笔记与 AI 搭档

#### TASK-017 笔记、时间线、关系图和搭档引用

- **所属阶段：** 阶段 7；**优先级：** P0；**任务类型：** 前端/后端/AI
- **任务目标：** 提供可恢复的推理工作台。
- **具体需求：** CRUD、自动保存、事实/陈述/推测/假设分类、证据来源、时间线、关系图、授权开关、搭档摘要/矛盾/建议和四档帮助。
- **前置依赖：** TASK-006、TASK-012、TASK-014；**输入：** 玩家投影和笔记；**输出：** Note、PartnerAnalysis、引用。
- **涉及文件/API：** `api/database_api.py`、notes/partner API；`EnhancedNotesPanel.tsx`、`components/notes/`。
- **实现步骤：** 版本保存；IndexedDB 草稿；同步冲突副本；引用校验；搭档不读答案；帮助消耗。
- **验收标准：** 断网不丢本地笔记；搭档引用仅指向已知内容；分析不会改变笔记。
- **测试要求：** 冲突同步、越权引用、长笔记、移动端键盘测试。
- **风险：** 用户推测被当事实；**回滚：** 强制显示来源类型并允许撤销自动标注。
- **预计工作量：** 待评估；**负责人角色：** 前端/后端/AI。
- **完成定义：** 调查、笔记、搭档三者可完成可追溯闭环。

### 阶段 8：自定义剧本编辑器

#### TASK-018 编辑器数据与画布

- **所属阶段：** 阶段 8；**优先级：** P1；**任务类型：** 前端/后端
- **任务目标：** 提供私人剧本配置、拖拽和版本工作流。
- **具体需求：** 基本信息、章节、场景、角色、关系、事实、时间线、证物、节点、触发器、结局；草稿、预览、测试、冻结、导入导出。
- **前置依赖：** TASK-007、TASK-008、TASK-017；**输入：** 内容 Schema；**输出：** `ScriptEditor.tsx`、子组件、脚本 API。
- **实现步骤：** React Flow 画布；属性面板；增量保存；草稿版本；只在质检通过后冻结。
- **验收标准：** 可创建并预览合成案件；非法连接有提示；冻结版不可直接覆盖。
- **测试要求：** 节点拖拽、保存恢复、导入导出和权限测试。
- **风险：** 编辑器过早复杂化；**回滚：** MVP 先支持表单和基础节点。
- **预计工作量：** 待评估；**负责人角色：** 前端/后端/内容。
- **完成定义：** 一位非开发作者可完成最小剧本。

#### TASK-019 AI 创作与剧本质检

- **所属阶段：** 阶段 8；**优先级：** P1；**任务类型：** AI/内容/后端
- **任务目标：** 生成草稿并定位阻断、严重和建议问题。
- **具体需求：** 大纲、角色、秘密、动机、手法、对白、证物、结局、润色；检查时间线、可达性、证据覆盖、越权、分支死路、敏感内容。
- **前置依赖：** TASK-015、TASK-018；**输入：** 作者主题和草稿；**输出：** 差异草稿、ValidationReport。
- **实现步骤：** AI 只写草稿；显示 diff；规则质检；有限状态遍历；人工确认后合并。
- **验收标准：** AI 不直接发布；阻断问题阻止冻结；报告定位到节点。
- **测试要求：** 故意错误剧本、长内容、敏感内容和重试。
- **风险：** 自动质检漏报；**回滚：** 人工审核和最近稳定版本。
- **预计工作量：** 待评估；**负责人角色：** AI/内容/测试。
- **完成定义：** 三个合成剧本可生成报告并导出。

### 阶段 9：AI 内容生成

#### TASK-020 媒体任务、资源版本和四类生成器

- **所属阶段：** 阶段 9；**优先级：** P1；**任务类型：** AI/后端/运维
- **任务目标：** 支持头像、封面、场景背景和证物图像的异步生成。
- **具体需求：** 队列、取消、重试、版本、审核、私有对象存储或受控目录、MIME/大小/路径校验。
- **前置依赖：** TASK-006、TASK-018；**输入：** 生成请求；**输出：** `avatar_generator.py`、`cover_generator.py`、`background_generator.py`、`evidence_generator.py`、Asset/Job。
- **实现步骤：** 统一 Job 协议；队列 Worker；签名 URL；审核状态；失败清理；证物关键文字用模板渲染。
- **验收标准：** 未审核资源不可公开；删除会清理任务和对象；重复任务不重复扣账。
- **测试要求：** 大文件、恶意路径、Provider 失败、取消、权限和过期 URL。
- **风险：** 生成质量和费用；**回滚：** 文字占位或旧版资源。
- **预计工作量：** 待评估；**负责人角色：** AI/后端/运维。
- **完成定义：** 四类生成任务均有状态、预览、审核和替换流程。

### 阶段 10：前端页面与交互

#### TASK-021 主要页面与组件状态

- **所属阶段：** 阶段 10；**优先级：** P0；**任务类型：** 前端
- **任务目标：** 完成用户可操作的单人产品界面。
- **具体需求：** `Home.tsx`、`ScriptLibrary.tsx`、`ScriptEditor.tsx`、`ScriptGenerator.tsx`、`PlayScript.tsx`、`DocsPage.tsx`、`Actor.tsx`、`EnhancedNotesPanel.tsx`、`MultipleChoiceGame.tsx` 及 evidence/notes/编辑器/生成器组件。
- **前置依赖：** TASK-009—017 的接口契约；**输入：** OpenAPI/DTO；**输出：** 页面、providers、api、types、utils。
- **实现步骤：** 先接假数据；实现加载/空/错误/流式/离线/冲突；接真实 API；响应式与无障碍。
- **验收标准：** 页面不暴露私密字段；手机和桌面可完成核心流程；键盘可操作；SSE 中断可恢复。
- **测试要求：** Playwright、移动 viewport、键盘、屏幕阅读器抽查。
- **风险：** API 变更；**回滚：** 使用版本化 DTO 和 Mock Adapter。
- **预计工作量：** 待评估；**负责人角色：** 前端。
- **完成定义：** UI 状态矩阵和页面验收签收。

### 阶段 11：安全、隐私、日志和运维

#### TASK-022 身份、资源权限、密钥和隐私

- **所属阶段：** 阶段 11；**优先级：** P0；**任务类型：** 安全/后端/运维
- **任务目标：** 保护玩家存档、角色私密信息和 Provider 密钥。
- **具体需求：** Cookie/游客会话、所有权、SSE 鉴权、签名资源、Prompt Injection 防护、Key 管理、IndexedDB 清理、导出删除。
- **前置依赖：** TASK-005、TASK-014、TASK-020；**输入：** 隐私和日志规范；**输出：** 鉴权中间件、删除任务、CSP 和安全文档。
- **实现步骤：** 默认拒绝权限；脱敏日志；密钥不落前端；对象访问校验；删除撤销任务并清理资产。
- **验收标准：** IDOR、越权、密钥泄露、缓存残留和路径穿越测试无高危问题。
- **测试要求：** OWASP、依赖扫描、密钥扫描、删除恢复演练。
- **风险：** 外部 Provider 保留数据；**回滚：** 禁用相关 Provider/能力并提示用户。
- **预计工作量：** 待评估；**负责人角色：** 安全/后端/运维。
- **完成定义：** 隐私清单、威胁模型和处理流程签收。

#### TASK-023 监控、备份、告警和发布脚本

- **所属阶段：** 阶段 11；**优先级：** P0；**任务类型：** 运维
- **任务目标：** 提供可观察、可恢复的生产基线。
- **具体需求：** 健康/就绪检查、请求 ID、Trace、模型成本、队列积压、审核指标、备份、迁移、灰度、回滚。
- **前置依赖：** TASK-002、TASK-005、TASK-015、TASK-020；**输入：** 部署环境；**输出：** Docker/Compose、监控配置、运维手册。
- **实现步骤：** 配置结构化日志；告警阈值；数据库时间点恢复；发布前后检查；Provider 熔断。
- **验收标准：** DB/Redis/Provider 故障有明确降级；备份恢复演练成功；回滚不破坏旧会话。
- **测试要求：** 故障注入、进程重启、恢复点和告警测试。
- **风险：** 监控含敏感内容；**回滚：** 关闭高风险采集字段并轮换凭证。
- **预计工作量：** 待评估；**负责人角色：** 运维/技术负责人。
- **完成定义：** 发布、故障、恢复和回滚手册可演练。

### 阶段 12：测试与质量保障

#### TASK-024 全量自动化与 AI 评测

- **所属阶段：** 阶段 12；**优先级：** P0；**任务类型：** 测试
- **任务目标：** 以分层测试验证功能、质量、安全和兼容性。
- **具体需求：** 单元、API 合约、DB 集成、规则、多智能体、防泄露、证物、笔记搭档、编辑器、媒体、SSE、断网、并发、E2E、浏览器、移动、无障碍、性能和安全。
- **前置依赖：** TASK-004—023；**输入：** 测试剧本和攻击集；**输出：** CI 报告、缺陷清单和质量门禁。
- **实现步骤：** 建 fixtures；模拟 Provider；真实 Provider 小样本；k6 压测；Playwright；人工三本剧本验收。
- **验收标准：** 核心回归无越权外发；重复/乱序不产生重复效果；阻断缺陷为零。
- **测试要求：** 攻击集至少 300 条、合法披露集至少 200 条，阈值以 M0 确认。
- **风险：** 评测样本偏差；**回滚：** 扩大样本并人工复核，不绕过门禁。
- **预计工作量：** 待评估；**负责人角色：** 测试/AI/内容。
- **完成定义：** 测试报告、性能报告、人工签收和已知限制齐备。

### 阶段 13：发布与上线

#### TASK-025 生产发布与上线观察

- **所属阶段：** 阶段 13；**优先级：** P0；**任务类型：** 运维/产品/内容
- **任务目标：** 安全发布 MVP 并观察真实运行。
- **具体需求：** 生产配置、迁移、内容冻结、三本剧本导入、模型预算、监控、备份、灰度、回滚、事故流程。
- **前置依赖：** TASK-024；**输入：** 已签收镜像、内容包和手册；**输出：** 发布记录、观察报告和问题清单。
- **实现步骤：** 预发布演练；冻结 ScriptVersion；迁移和导入；小流量发布；观察错误、延迟、费用、审核和完成率；必要时回滚。
- **验收标准：** 发布前清单全通过；核心指标可见；回滚演练有效；无未处理阻断问题。
- **测试要求：** Smoke、真实三本剧本、备份恢复和事故演练。
- **风险：** Provider 或内容问题；**回滚：** 回到上一应用镜像/内容版本，取消未完成任务。
- **预计工作量：** 待评估；**负责人角色：** 运维/产品/技术负责人。
- **完成定义：** 负责人共同签收上线与后续观察窗口。

## 6. 数据库实施顺序

1. 身份、用户、剧本和版本。
2. 章节、场景、角色、事实、权限和关系。
3. 会话、状态快照、调查事件和角色状态。
4. 证物定义、实例、组合和反应。
5. 对话运行、批准消息块、记忆和陈述。
6. 笔记、搭档分析、提交、评分和结局。
7. 资产、生成任务、使用量、Outbox 和审计。
8. 索引、分区/归档评估、种子和恢复测试。

## 7. API 实施顺序

1. `/health`、错误协议、请求 ID、身份和剧本查询。
2. `/sessions`、`/state`、`/actions`。
3. `/dialogue-runs`、SSE、取消、重试和恢复。
4. `/evidence`、`/notes`、`/partner-analyses`。
5. `/submissions`、`/reveal`、历史和重玩。
6. `/scripts` 编辑、校验、版本和冻结。
7. `/media-jobs`、资源签名 URL、生成器状态。
8. 隐私导出/删除、监控和内部质检接口。

每个接口上线前必须冻结请求/响应 Schema、鉴权字段、错误码、幂等规则和审计事件。

## 8. 前端实施顺序

1. providers、types、api、utils 和错误边界。
2. Home 与 ScriptLibrary：筛选、详情、开局。
3. PlayScript：场景、角色、对话和 SSE。
4. evidence 与 notes：证物、笔记、时间线、关系图。
5. Actor 与 EnhancedNotesPanel：状态和搭档。
6. MultipleChoiceGame：结算、真相和多周目。
7. ScriptEditor 和 ScriptGenerator：先表单，再画布和 AI 草稿。
8. DocsPage、设置、隐私和响应式/无障碍收尾。

## 9. AI 能力实施顺序

1. Provider Adapter、模拟模型、计量和超时。
2. 授权投影和角色上下文构建。
3. 结构化候选、生成和基础审核。
4. 修订、复检、安全回避和指标。
5. 记忆摘要、陈述台账、情绪/信任策略。
6. 搭档分析和引用验证。
7. 剧本创作、质检和媒体生成。

## 10. 测试计划与质量门禁

- 任何 P0 功能必须有单元、接口和至少一条 E2E 测试。
- 任何状态变更必须测试重复、乱序、并发、取消和过期版本。
- 防泄露以攻击集、合法披露集、人工抽检联合评测；核心越权外发阻断发布。
- 性能报告分别列 API、原始 TTFT、审核后首块、总完成、SSE 恢复、存档成功率和 Provider 失败率。
- 三个正式剧本必须完成成功、错误结算、隐藏证物和替代调查路径验收。
- 发布门禁：阻断缺陷为零，安全高危为零，迁移/备份/回滚演练通过，隐私日志检查通过。

## 11. 环境、部署和运维计划

开发环境使用 Docker Compose、模拟 Provider 和合成剧本；测试环境使用隔离数据库、Redis、对象存储和受控真实模型额度；预发布环境使用正式构建和三本候选剧本；生产环境使用独立密钥、私有资源桶、备份、告警和最小权限账号。发布采用迁移先行、内容版本冻结、镜像灰度和可回滚配置。普通日志不记录对话全文、笔记、Prompt、私密事实、音频或密钥。

## 12. 风险、阻塞项和决策清单

| 项目 | 当前处理 | 阻塞条件 |
|---|---|---|
| 审核后延迟 | M0 实测后确定产品承诺 | 无法接受则调整模型/交互 |
| Provider 地区/价格 | 部署前验证 | 无可用 Provider 则使用模拟或备选 |
| 防泄露阈值 | 冻结评测集后确定 | 核心漏检不得上线 |
| 内容可解性 | 规则质检 + 人工试玩 | 关键线索不可达阻断 |
| 图像文字一致性 | 关键文字模板渲染 | 失败则使用文字证物 |
| 编辑器范围 | MVP 先基础表单 | 画布影响主流程则延期 V1.5 |
| 精确排期 | 待人员和内容确认 | 未确认不承诺人日 |

## 13. MVP 首个可运行版本范围

MVP 包含：游客/账号、剧本库、至少三个固定版本剧本、开局与存档、场景调查、角色多轮对话、审核后流式块、中断恢复、证物基础操作、文本推理笔记、四档搭档提示、结构化结算、真相揭示、多结局基础版、GPT/DeepSeek Provider、Docker 部署和基础监控。自定义编辑器的可视化画布、语音、复杂媒体生成、动态案件、本地模型和所有社交功能不属于首个可运行版本。

## 14. 技术完成定义与发布检查清单

- [ ] 所有 P0 任务完成并通过代码评审、测试和文档检查。
- [ ] 服务端权威控制真相、权限、证物、评分和结局；前端无未授权答案。
- [ ] 未审核候选不进入 SSE、语音、公开资源和普通日志。
- [ ] 角色、搭档、作者和玩家投影权限测试通过。
- [ ] 重复/乱序/取消/断网/重启不产生重复效果或丢失已提交事件。
- [ ] 三本剧本完成成功、错误和替代路径人工验收。
- [ ] 攻击集、合法披露集、性能、兼容、无障碍和安全报告已归档。
- [ ] 数据迁移、备份恢复、删除导出、密钥轮换和回滚演练完成。
- [ ] 监控、告警、预算、Provider 降级和事故流程已启用。
- [ ] README、API 文档、内容编写规范和操作手册已更新。

## 15. 待团队确认的问题

1. 首发部署地区、账号渠道和正式身份认证方式是什么？
2. OpenAI、DeepSeek 的具体模型、额度、预算和数据保留策略是什么？
3. MVP 是否必须提供语音输入/播放，还是延后到 V1.5？
4. 三本首发剧本的负责人、版权、素材和内容分级是否已确定？
5. 调查点、提示次数、重试次数和多结局规则的产品数值是什么？
6. 目标并发会话数、P95 延迟、RPO/RTO 和费用上限是什么？
7. 自定义编辑器 MVP 是否接受基础表单版而非完整拖拽画布？
8. 数据保存、导出、删除和外部模型区域要求由谁最终确认？
