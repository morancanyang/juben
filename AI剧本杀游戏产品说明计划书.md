# 《夜半卷宗》AI剧本杀游戏产品说明计划书

**文档版本：** V1.0  
**文档状态：** 当前 MVP 说明与后续实施计划  
**更新时间：** 2026-09-14  
**适用场景：** 比赛答辩、产品展示、研发协作、后续版本规划

## 1. 产品概述

《夜半卷宗》是一款单人 AI 互动推理游戏。玩家独自进入案件现场，通过搜索场景、发现和分析证物、分别询问 NPC、整理笔记并提交结案报告，逐步还原案件真相。

产品的核心体验不是阅读固定剧本，而是与一组拥有不同职业、目标、顾虑、记忆和表达方式的 NPC 进行调查。每个 NPC 都有自己的 Agent 状态，玩家的提问、信任变化、重复追问和出示证物都会影响其回应方式。

## 2. 产品定位

### 2.1 一句话定位

一个人进入案件世界，与拥有独立思维的 AI 角色周旋，依靠证据链亲手拼出真相。

### 2.2 目标用户

| 用户类型 | 主要需求 |
|---|---|
| 推理爱好者 | 高自由度提问、严谨证据链和可复核结论 |
| 互动故事玩家 | 有氛围的角色对话和清晰的新手引导 |
| 独处娱乐用户 | 随时开始、自动保存、无需组局 |
| 剧本创作者 | 不写代码也能制作和检查案件 |
| 比赛评审与观众 | 能快速理解产品创新点并看到完整演示闭环 |

### 2.3 产品边界

当前产品坚持单人调查定位，不包含多人联机、好友私聊、社区投稿、排行榜和多人房间。AI 是游戏角色和调查搭档，不替玩家直接决定结论。

## 3. 核心创新

### 3.1 每个 NPC 都是独立 Agent

每个 NPC 拥有独立的：

- 职业身份、公开背景和性格；
- 核心目标、恐惧、利益关系和事件立场；
- 说话节奏、用词习惯和面对质疑时的反应策略；
- 信任度、警觉度、情绪和自己的对话历史；
- 讨论主题、重复追问、信任变化和玩家说法等私有记忆。

NPC 只读取自己的聊天历史和授权内容，不读取其他 NPC 的私密对话。DeepSeek 在当前版本中通过角色 Prompt、私有记忆和规则审核实现 Agent 行为，不修改模型权重。

### 3.2 证据优先的调查机制

游戏中的真相、证物权限、事件条件、调查点、评分和结局由服务端规则控制。AI 只能在当前角色获准的证词目录内组织语言，不能自行发放隐藏证物或修改标准答案。

### 3.3 电影化调查体验

场景图、证物影像、NPC 职业头像、案件封面和粒子消散动画共同构成深色电影风格。玩家点击证物时查看影像，关闭影像后会以粒子逐步消散，增强“证据正在离开现场”的沉浸感。

## 4. 当前游戏内容

### 4.1 正式案件

| 剧本 | 主题 | NPC | 场景 | 证物 |
|---|---|---:|---:|---:|
| 雨夜山庄 | 密室、红茶、旧账 | 3 | 3 | 8 |
| 末班列车 | 断电、录音、时间差 | 3 | 3 | 8 |
| 无声回响 | 极夜、权限日志、假消息 | 3 | 3 | 8 |

正式内容共 3 个案件、9 名 NPC、9 个场景和 24 件证物。另有教学案件模板，包含 2 名 NPC 和 3 件证物；连同教学内容共有 11 名 NPC、27 件证物影像。

### 4.2 单局流程

1. 游客或账号进入游戏。
2. 选择剧本和难度，阅读案件导入。
3. 访问场景并调查可操作对象。
4. 获得、分析、组合和出示证物。
5. 分别进入 NPC 的一对一聊天室进行询问。
6. 记录事实、角色陈述、玩家推测和未验证假设。
7. 调用 AI 搭档进行已知内容范围内的总结和提示。
8. 提交凶手、动机、时间、地点、工具、共犯和关键证据。
9. 查看评分、遗漏线索、分章真相和结局。

## 5. 产品功能规划

### 5.1 当前 MVP 已实现

- 游客会话、注册登录、游客升级和个人档案；
- 剧本库、案件详情、开局配置和断点续玩；
- 场景调查、调查点、事件条件、证物分析/组合/出示；
- NPC 独立 Agent、信任/警觉/情绪和私有记忆；
- DeepSeek `deepseek-flash`、本地演示 Provider 和 OpenAI Provider；
- 生成、审核、修订、复检后交付的语义块对话；
- SSE 进度、取消、重试、过期版本保护和自动存档；
- 证物影像、场景图、NPC 头像、一对一聊天室和左右消息分栏；
- 文本笔记、证词引用、证物关联、离线草稿和冲突副本；
- AI 搭档分析、结构化结算、评分、隐藏线索和分章真相；
- 私人剧本工作台、模板、JSON 导入导出、质检和冻结版本。

### 5.2 后续版本规划

- 语音输入、角色语音播放和更丰富的调查动作；
- 完整拖拽式剧情画布和复杂分支编辑；
- AI 剧本草稿生成、差异审阅和批量素材生成；
- 更细的多结局、长期记忆和多视角多周目；
- PostgreSQL、Redis/Worker、对象存储和生产级监控；
- 本地模型、视频 NPC 和动态案件生成。

## 6. AI Agent 工作方式

### 6.1 输入信息

一次 NPC 对话只向模型提供：

- 当前 NPC 的身份、职位、性格和 Agent Profile；
- 当前 NPC 的信任度、警觉度、情绪和私有记忆；
- 当前 NPC 满足条件的授权证词；
- 该 NPC 最近的聊天历史；
- 当前场景、已发现/已分析证物和调查时间；
- 玩家本轮问题。

完整答案、隐藏证据、其他 NPC 历史和未授权私密事实不会进入该 NPC 的模型上下文。

### 6.2 输出流程

模型返回结构化候选：

```json
{
  "statement_ids": ["lin_1"],
  "tone": "calm",
  "reply": "自然改写后的角色回答"
}
```

服务端会检查字段格式、证词 ID 权限、重复声明和角色事实边界。候选不合格时最多进行两轮修订，仍失败则使用安全回避语，不把未审核内容直接交给玩家。

### 6.3 记忆与状态变化

玩家重复追问会提高 NPC 警觉并降低信任；正常交流会逐步改善信任。系统记录每个 NPC 的最近问题、讨论主题、重复追问、信任历史和玩家曾提出的说法。不同 NPC 之间不会共享这些记忆。

## 7. 技术与部署说明

### 7.1 当前运行架构

- 前端：React + Vite PWA；
- 后端：FastAPI + SQLAlchemy；
- 本地数据库：SQLite，文件位于 `runtime/game.db`；
- 对话任务：API 进程内异步任务 + DialogueRun 状态机；
- 交付方式：SSE 读取状态和已审核语义块；
- 静态媒体：前端 `web/src/media/` 资源。

### 7.2 启动方式

比赛联调推荐使用：

```powershell
cd C:\Users\33864\Desktop\aijuben
.venv\Scripts\python run_server_env.py
```

服务端端口为 `8765`。日常开发也可以按 README 使用 uvicorn `8000` 和 Vite `5173`。项目根目录 `.env` 是模型配置的权威覆盖文件，当前 DeepSeek 使用 `deepseek-flash`。

### 7.3 安全边界

API Key 只保存在服务端环境变量中，不进入前端。前端只能读取玩家投影后的状态，不能直接访问完整剧本答案、隐藏证物条件或其他 NPC 的私密记忆。

## 8. 比赛演示计划

### 8.1 推荐演示时长：5—8 分钟

| 时间 | 演示内容 | 展示重点 |
|---:|---|---|
| 1 分钟 | 首页、案件选择和电影化封面 | 产品定位与视觉风格 |
| 2 分钟 | 调查场景、点击证物和粒子消散 | 证据先于答案、交互反馈 |
| 2 分钟 | 依次询问两名不同 NPC | 同问不同答、职位语气和 Agent 差异 |
| 1 分钟 | 出示证物并观察 NPC 态度变化 | 证物授权、信任和警觉 |
| 1—2 分钟 | 笔记、搭档分析和结案揭示 | 完整闭环和可复核评分 |

### 8.2 演示注意事项

- 提前确认 DeepSeek 密钥、模型名称和网络状态；
- 如模型不可用，切换本地演示 Provider 保证流程可展示；
- 演示 NPC 独立性时，应向两个职位不同的角色提出同一问题；
- 优先展示已分析证物和可触发角色反应的证物；
- 不在公开投屏中展示 `.env`、API Key、完整答案或后台审核日志。

## 9. 项目实施计划

### 9.1 当前阶段：比赛版 MVP

目标是保证三本正式案件完整可玩，重点验证：

- 角色 Agent 能否保持独立人格和私有记忆；
- 无关问题是否会被正确回避；
- 证物解锁和 NPC 授权是否一致；
- 对话中断、重试和自动保存是否可靠；
- 页面在桌面和移动端是否能完成核心流程。

### 9.2 下一阶段：稳定性与真实模型评测

1. 使用有效 DeepSeek 配置完成多 NPC 真实网络回归。
2. 建立同一问题跨角色、重复追问、越权问题、闲聊和未解锁证物评测集。
3. 记录真实模型延迟、费用、修订比例、误拦截和越权风险。
4. 通过人工抽检后，确定真实 Provider 的上线门槛。

### 9.3 后续版本：创作与内容扩展

1. 完善私人剧本编辑器和章节画布。
2. 增加 AI 辅助创作、质检和素材版本管理。
3. 扩展语音、长期记忆、多结局和多周目。
4. 根据并发量和费用评估 PostgreSQL、Redis/Worker 和对象存储迁移。

## 10. 验收标准

### 10.1 功能验收

- 新用户可以从进入游戏走到结局揭示；
- 三本正式案件均可发现证物、询问 NPC、提交结案；
- 每个 NPC 的聊天记录、状态和记忆彼此隔离；
- 证物点击、分析、出示、组合和影像关闭流程正常；
- 剧本工作台可以保存私人草稿并恢复；
- 重复请求不会重复扣调查点或重复发放证物。

### 10.2 AI 验收

- NPC 回复符合自己的职位、Agent Profile 和当前状态；
- NPC 不读取其他角色的聊天内容；
- 未解锁证物和完整答案不会被提前泄露；
- 无关问题不会误返回案件证词；
- 模型失败时不会把原始未审核结果展示给玩家。

### 10.3 测试边界

当前后端 24 项 pytest 和前端构建已通过，主要验证规则、权限、状态机和界面流程。自动化测试默认使用 mock Provider，因此不能直接代表真实 DeepSeek 的延迟、防泄露率或成本表现。真实模型上线前仍需完成攻击集、合法披露集、重复对话和人工抽检。

## 11. 风险与应对

| 风险 | 应对方式 |
|---|---|
| 模型泄露谜底 | 最小授权上下文、结构化证词 ID、审核和安全回避 |
| NPC 变成固定台词 | 独立 Profile、私有记忆、信任/警觉状态和真实模型回归 |
| 模型返回格式错误 | Schema 校验、最多两轮修订和安全降级 |
| 证物条件错误 | 服务端规则引擎、幂等事务和可重放事件 |
| 延迟或费用过高 | 调用额度、超时重试、语义块交付和 Provider 降级 |
| 素材风格不一致 | 案件/角色/证物资源 ID、统一提示词和人工审核 |
| 玩家卡关 | 非剧透提示、搭档分层分析和可复核笔记 |

## 12. 文档维护规则

- 任何新增 NPC 必须补充独立 Agent Profile、授权证词和初始状态；
- 任何新增证物必须同步描述、分析、发现条件、影像和事件关联；
- 任何模型或 Provider 变更必须同步 `.env.example`、部署说明和测试记录；
- 已实现功能与规划功能必须分开描述；
- 结算规则、权限规则和存档结构变更必须同步 PRD、技术方案和实施计划；
- 真实模型评测结果必须记录 Provider、模型、样本量、时间和环境。

## 13. 关联文档

- [产品需求文档](AI剧本杀游戏产品需求文档-精细版.md)
- [技术与实施方案](AI剧本杀游戏-技术与实施方案.md)
- [实施计划](AI剧本杀游戏-实施计划.md)
- [DeepSeek 接入说明](docs/deepseek-integration.md)
- [NPC 头像与一对一聊天室](docs/npc-avatars.md)
- [证物影像与粒子消散](docs/evidence-images.md)
